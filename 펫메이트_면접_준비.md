# 펫메이트 프로젝트 면접 질문 & 답변

## 📋 목차
1. [프로젝트 개요 및 담당 역할](#1-프로젝트-개요-및-담당-역할)
2. [지도 페이지 구현](#2-지도-페이지-구현)
3. [업체 관리 구현](#3-업체-관리-구현)
4. [주소 관리 구현](#4-주소-관리-구현)
5. [트러블 슈팅](#5-트러블-슈팅)
6. [기술적 의사결정](#6-기술적-의사결정)

---

## 1. 프로젝트 개요 및 담당 역할

### Q1-1: 펫메이트 프로젝트에 대해 소개해주세요.

**답변:**
펫메이트는 반려동물 서비스(돌봄, 산책, 미용, 병원)를 지도 기반으로 검색하고 예약할 수 있는 O2O 플랫폼입니다. React를 사용한 SPA 방식으로 개발했으며, 카카오맵 API를 활용해 위치 기반 서비스를 제공합니다.

저는 프론트엔드 개발을 담당했으며, 특히 **지도 페이지**, **업체 관리**, **주소 관리** 3가지 핵심 기능을 구현했습니다.

**꼬리질문 1-1-1:** 왜 React를 선택했나요?
> **답변:** React는 컴포넌트 기반 아키텍처로 재사용성이 높고, Virtual DOM을 통해 지도와 같이 빈번한 업데이트가 발생하는 UI에서도 효율적인 렌더링이 가능합니다. 또한 풍부한 생태계와 카카오맵 API와의 통합 레퍼런스가 많아 선택했습니다.

**꼬리질문 1-1-2:** SPA의 단점은 무엇이고 어떻게 보완했나요?
> **답변:** SPA의 주요 단점은 초기 로딩 속도와 SEO입니다. 초기 로딩은 lazy loading과 코드 스플리팅으로 개선했고, SEO는 공개 API 엔드포인트(`/api/company/public/{id}`)를 별도로 제공해 서버사이드에서 메타데이터를 렌더링할 수 있도록 대응했습니다.

---

### Q1-2: 팀에서 어떤 역할을 맡았고, 협업은 어떻게 진행했나요?

**답변:**
프론트엔드 개발자로 참여했으며, 백엔드 개발자와 RESTful API 명세를 먼저 정의한 후 병렬로 개발했습니다. Git을 통해 코드를 관리했고, 기능 단위로 브랜치를 분리하여 작업했습니다.

API 연동 시에는 먼저 Mock 데이터로 UI를 완성한 후, 백엔드 API가 완성되면 실제 연동하는 방식으로 진행했습니다.

**꼬리질문 1-2-1:** API 명세는 어떻게 관리했나요?
> **답변:** 초기에는 구두로 소통했으나, 나중에는 Swagger 문서를 활용해 명세를 공유했습니다. 또한 프론트엔드 코드에서 `services/` 디렉토리에 API 함수들을 모듈화하여 관리했습니다.

**꼬리질문 1-2-2:** 충돌이 발생하면 어떻게 해결했나요?
> **답변:** 기능 단위로 브랜치를 분리하고, 파일 단위로 책임을 명확히 분리해 충돌을 최소화했습니다. 충돌이 발생하면 코드 리뷰를 통해 해결했으며, 공통 컴포넌트는 먼저 합의 후 작업했습니다.

---

## 2. 지도 페이지 구현

### Q2-1: 지도 페이지의 핵심 기능은 무엇인가요?

**답변:**
지도 페이지는 사용자의 위치를 기반으로 근처 업체를 찾는 기능입니다. 주요 기능은 다음과 같습니다:

1. **위치 기반 업체 검색**: 사용자 위치 또는 드래그한 위치 기준으로 반경 내 업체 조회
2. **서비스별 필터링**: 돌봄, 산책, 미용, 병원 등 서비스 타입별 필터
3. **마커 클러스터링 및 선택**: 업체별 마커 표시, 클릭 시 상세 정보 표시
4. **줌 레벨별 검색 반경 자동 조정**: 줌 레벨에 따라 0.5~4km 반경으로 자동 조정
5. **드래그 가능한 사용자 마커**: 마커를 드래그하여 위치 변경 및 재검색

**파일 경로:**
- `src/pages/common/Map/MapPage.jsx` - 메인 컨테이너, 상태 관리
- `src/pages/common/Map/components/MapContainer.jsx` - 지도 렌더링, 마커 관리

**꼬리질문 2-1-1:** 카카오맵 API를 어떻게 초기화했나요?
> **답변:**
> ```javascript
> useEffect(() => {
>   const script = document.createElement("script");
>   script.src = `//dapi.kakao.com/v2/maps/sdk.js?appkey=${process.env.REACT_APP_KAKAO_MAP_KEY}&libraries=services&autoload=false`;
>   script.async = true;
>   script.onload = () => window.kakao.maps.load(() => setIsKakaoLoaded(true));
>   document.head.appendChild(script);
> }, []);
> ```
> `autoload=false`로 설정하고 `window.kakao.maps.load()`로 명시적으로 초기화했습니다. 이렇게 하면 스크립트 로딩 타이밍을 정확히 제어할 수 있습니다.

**꼬리질문 2-1-2:** 초기 위치는 어떻게 결정하나요?
> **답변:** 우선순위는 다음과 같습니다:
> 1. **사용자의 기본 주소** (DB에서 `getAddressesByDefault()` 조회)
> 2. **GPS 위치** (`navigator.geolocation` API)
> 3. **기본값** (서울시청: 37.5665, 126.978)
>
> 비동기 처리로 순차적으로 시도하며, 실패 시 다음 방법으로 fallback합니다.

---

### Q2-2: 마커는 어떻게 관리하나요?

**답변:**
마커는 **useRef**로 관리하여 리렌더링 시에도 참조를 유지합니다. 주요 마커는 2가지입니다:

1. **사용자 위치 마커** (파란색 원형)
   - Canvas로 커스텀 이미지 생성
   - 드래그 가능 (`setDraggable(true)`)
   - 드래그 종료 시 새 위치로 업체 재조회

2. **업체 마커** (서비스별 아이콘)
   - 선택 전: 원형 마커 (`createCircleMarker`)
   - 선택 후: 물방울 모양 마커 (`createDropMarker`)
   - 클릭 이벤트로 상세 정보 표시

```javascript
// 마커 저장소
const markersRef = useRef([]);              // 업체 마커들
const labelsRef = useRef([]);               // 업체명 라벨들
const userMarkerRef = useRef(null);         // 사용자 마커
const currentSelectedMarkerRef = useRef(null); // 선택된 마커
```

**파일:** `src/pages/common/Map/components/MapContainer.jsx` (라인 566-686)

**꼬리질문 2-2-1:** useState가 아니라 useRef를 사용한 이유는?
> **답변:** 마커 객체는 카카오맵 API의 네이티브 객체이므로, React 상태로 관리하면 불필요한 리렌더링이 발생합니다. useRef를 사용하면 참조만 유지하고 렌더링은 트리거하지 않아 성능이 좋습니다.

**꼬리질문 2-2-2:** 마커 이미지는 어떻게 생성하나요?
> **답변:** Canvas API를 사용해 동적으로 생성합니다:
> ```javascript
> const createCircleMarker = (icon, color) => {
>   const canvas = document.createElement('canvas');
>   const ctx = canvas.getContext('2d');
>   // 원형 배경 그리기
>   ctx.fillStyle = color;
>   ctx.arc(25, 25, 20, 0, Math.PI * 2);
>   ctx.fill();
>   // 아이콘 이미지 그리기
>   const img = new Image();
>   img.src = icon;
>   img.onload = () => ctx.drawImage(img, 10, 10, 30, 30);
>   return canvas.toDataURL();
> };
> ```
> 이 방식은 서비스 타입별로 동적으로 색상과 아이콘을 변경할 수 있어 유연합니다.

---

### Q2-3: 필터링은 어떻게 구현했나요?

**답변:**
필터링은 **서비스 타입**과 **검색어** 2가지 방식을 지원합니다.

1. **서비스 필터** (전체/돌봄/산책/미용/병원/기타)
   - 선택한 서비스 타입을 state로 관리
   - `useMemo`로 필터링된 목록 계산 (메모이제이션)
   - API 요청 시 `serviceType` 파라미터 전달

```javascript
const displayCompanies = useMemo(() => {
  const baseCompanies = filteredCompanies.length > 0 ? filteredCompanies : companies;
  if (selectedService === null) {
    return baseCompanies;
  }
  return baseCompanies.filter(company => company.repService === selectedService);
}, [filteredCompanies, companies, selectedService]);
```

2. **주소 검색**
   - 카카오맵 Geocoder로 주소 → 좌표 변환
   - 주소 검색 실패 시 업체명 검색으로 fallback

**파일:** `src/pages/common/Map/MapPage.jsx` (라인 255-265)

**꼬리질문 2-3-1:** useMemo를 사용한 이유는?
> **답변:** 필터링 로직이 companies 배열 전체를 순회하기 때문에 비용이 큽니다. useMemo를 사용하면 의존성(`companies`, `selectedService`)이 변경될 때만 재계산하므로 불필요한 연산을 방지할 수 있습니다.

**꼬리질문 2-3-2:** 검색 성능을 더 개선할 방법은?
> **답변:**
> 1. **디바운싱**: 검색어 입력 시 500ms 지연 후 API 호출 (현재 구현됨)
> 2. **페이지네이션**: 현재는 최대 60개로 제한하지만, 무한 스크롤이나 페이지네이션 추가 가능
> 3. **서버 사이드 필터링**: 현재는 클라이언트에서 필터링하지만, API에 필터 파라미터를 전달해 서버에서 필터링하면 네트워크 트래픽을 줄일 수 있습니다.

---

### Q2-4: 지도 드래그/줌 변경 시 업체는 어떻게 업데이트되나요?

**답변:**
지도 이벤트 리스너를 등록하여 드래그나 줌 변경 시 업체를 재조회합니다. 단, 과도한 API 호출을 방지하기 위해 **디바운싱**(500ms)을 적용했습니다.

```javascript
useEffect(() => {
  if (!mapRef.current) return;

  window.kakao.maps.event.addListener(mapRef.current, 'dragend', handleMapChange);
  window.kakao.maps.event.addListener(mapRef.current, 'zoom_changed', handleMapChange);
}, []);

const handleMapChange = () => {
  if (debounceTimerRef.current) clearTimeout(debounceTimerRef.current);

  debounceTimerRef.current = setTimeout(() => {
    const markerPosition = userMarkerRef.current.getPosition();
    const zoomLevel = mapRef.current.getLevel();
    onLocationChange(markerPosition.getLat(), markerPosition.getLng(), zoomLevel);
  }, 500);
};
```

**파일:** `src/pages/common/Map/components/MapContainer.jsx` (라인 351-369)

**꼬리질문 2-4-1:** 디바운싱을 useRef로 관리한 이유는?
> **답변:** setTimeout의 ID를 저장해야 clearTimeout으로 취소할 수 있습니다. useRef를 사용하면 리렌더링과 무관하게 타이머 ID를 유지할 수 있고, 함수 스코프 변수보다 안정적입니다.

**꼬리질문 2-4-2:** 줌 레벨에 따라 검색 반경이 어떻게 변하나요?
> **답변:**
> - 줌 레벨 1~2: 반경 1km (최대 30개)
> - 줌 레벨 3~4: 반경 2km (최대 40개) - 기본값
> - 줌 레벨 5~6: 반경 3km (최대 50개)
> - 줌 레벨 7 이상: 반경 4km (최대 60개)
>
> 이렇게 하면 확대 시 가까운 업체만, 축소 시 넓은 범위의 업체를 볼 수 있습니다.

---

## 3. 업체 관리 구현

### Q3-1: 업체 관리 기능에 대해 설명해주세요.

**답변:**
업체 관리는 펫메이트 서비스 제공자가 자신의 업체를 등록하고 관리하는 기능입니다. 주요 기능은:

1. **업체 등록**: 업체 타입(개인/사업자) 선택, 기본 정보 입력, 서비스 선택, 운영시간 설정, 사진 업로드
2. **업체 목록 조회**: 내가 등록한 업체 목록 표시 (승인 상태 포함)
3. **업체 수정**: 기존 정보 수정 (동일한 폼 재사용)
4. **업체 삭제**: 삭제 확인 후 제거

**파일 경로:**
- `src/pages/company/CompanyManagePage.jsx` - 목록 조회, 삭제
- `src/pages/company/CompanyRegisterPage.jsx` - 등록, 수정
- `src/services/companyService.js` - API 호출

**꼬리질문 3-1-1:** 업체 타입(개인/사업자)에 따라 입력 폼이 어떻게 다른가요?
> **답변:**
> - **개인(일반인)**: 생년월일(YYMMDD), 성함
> - **개인/법인사업자**: 사업자등록번호, 상호명, 대표자명
>
> 타입 선택에 따라 조건부 렌더링으로 다른 필드를 표시합니다. 사업자는 사업자등록번호 중복 검증 API도 호출합니다.

**꼬리질문 3-1-2:** 등록과 수정 페이지를 어떻게 통합했나요?
> **답변:** URL 파라미터(`/companyregister/:id`)로 구분합니다. `id`가 있으면 수정 모드로 동작하며, `useEffect`에서 업체 정보를 불러와 폼에 채웁니다. `id`가 없으면 등록 모드로 빈 폼을 표시합니다.
> ```javascript
> const { id } = useParams();
> const isEditMode = !!id;
>
> useEffect(() => {
>   if (isEditMode) {
>     loadCompanyData(id);
>   }
> }, [id]);
> ```

---

### Q3-2: 업체 등록 시 주소는 어떻게 입력받나요?

**답변:**
주소 입력은 3가지 방법을 지원합니다:

1. **다음 우편번호 API**: 주소 검색 후 선택
2. **MapModal**: 지도에서 마커 드래그로 위치 선택
3. **직접 입력**: 좌표만 수동 입력 (비권장)

선택한 주소는 **카카오맵 Geocoder API**로 좌표(위도/경도)를 자동 추출합니다. 이 좌표는 지도에서 업체 마커 표시에 사용됩니다.

```javascript
const handleDaumPostcode = () => {
  new window.daum.Postcode({
    oncomplete: function(data) {
      const geocoder = new window.kakao.maps.services.Geocoder();
      geocoder.addressSearch(data.address, (result, status) => {
        if (status === window.kakao.maps.services.Status.OK) {
          setFormInputs({
            ...formInputs,
            roadAddr: data.address,
            postcode: data.zonecode,
            latitude: result[0].y,
            longitude: result[0].x
          });
        }
      });
    }
  }).open();
};
```

**꼬리질문 3-2-1:** 좌표가 필수인 이유는?
> **답변:** 지도에서 업체 마커를 정확한 위치에 표시하고, 사용자 위치 기반 근처 업체 검색(`/api/company/nearby`)에 사용하기 때문입니다. 주소만으로는 정확한 거리 계산이 불가능합니다.

**꼬리질문 3-2-2:** 좌표 추출이 실패하면 어떻게 처리하나요?
> **답변:** 현재는 에러 메시지를 표시하고 재시도를 유도합니다. 개선 방안으로는 서버에서 좌표를 추출하거나, 기본 좌표(구청 등)를 사용하는 방법이 있습니다.

---

### Q3-3: 제공 서비스와 운영시간은 어떻게 저장하나요?

**답변:**
제공 서비스와 운영시간은 **JSON 형태**로 저장합니다.

1. **제공 서비스** (`services` 필드)
```json
{
  "1": true,  // 돌봄
  "2": false, // 산책
  "3": true,  // 미용
  "4": false, // 병원
  "9": false  // 기타
}
```

2. **운영시간** (`operatingHours` 필드)
```json
{
  "월": { "start": "09:00", "end": "18:00", "closed": false },
  "화": { "start": "09:00", "end": "18:00", "closed": false },
  "수": { "closed": true },
  "목": { "start": "09:00", "end": "18:00", "closed": false },
  ...
}
```

프론트엔드에서는 체크박스와 시간 선택 UI로 입력받고, `JSON.stringify()`로 변환해서 API에 전송합니다.

**꼬리질문 3-3-1:** JSON 대신 관계형 테이블을 사용할 수도 있지 않나요?
> **답변:** 맞습니다. 정규화 관점에서는 별도 테이블(`company_services`, `operating_hours`)로 분리하는 것이 좋습니다. 하지만 이 데이터는:
> 1. 항상 업체와 함께 조회됨 (JOIN 오버헤드 발생)
> 2. 업데이트가 빈번하지 않음
> 3. 구조가 고정적임
>
> 따라서 JSON으로 저장해 조회 성능을 최적화했습니다.

**꼬리질문 3-3-2:** 24시간 운영은 어떻게 처리하나요?
> **답변:** `is24Hours` 플래그를 추가하고, `true`일 때는 `{ "start": "00:00", "end": "23:59" }`로 저장합니다. UI에서는 24시간 체크박스를 제공하고, 체크 시 시간 선택 필드를 비활성화합니다.

---

### Q3-4: 업체 사진 업로드는 어떻게 구현했나요?

**답변:**
업체 사진은 `ImageUploadViewer` 컴포넌트를 사용해 처리합니다:

1. **파일 선택**: `<input type="file" multiple accept="image/*">`
2. **미리보기**: FileReader API로 base64 변환 후 표시
3. **업로드**: FormData로 파일을 multipart/form-data 형식으로 전송
4. **저장**: 서버에서 파일 저장 후 URL 반환, DB에 JSON 배열로 저장

```javascript
const handleImageUpload = (e) => {
  const files = Array.from(e.target.files);
  const formData = new FormData();

  files.forEach(file => formData.append('images', file));

  uploadImages(formData).then(urls => {
    setFormInputs({
      ...formInputs,
      images: [...formInputs.images, ...urls]
    });
  });
};
```

**꼬리질문 3-4-1:** 이미지 크기 제한은 있나요?
> **답변:** 프론트엔드에서는 파일당 5MB로 제한하고, 이미지 개수는 최대 10개로 제한합니다. 서버에서도 동일한 검증을 수행합니다.

**꼬리질문 3-4-2:** 이미지 최적화는 어떻게 하나요?
> **답변:** Canvas API를 사용해 클라이언트에서 리사이징(최대 1920x1080) 후 업로드합니다. 추가로 서버에서 WebP 형식으로 변환하고, CDN을 통해 배포하면 로딩 속도를 더 개선할 수 있습니다.

---

## 4. 주소 관리 구현

### Q4-1: 주소 관리 기능에 대해 설명해주세요.

**답변:**
주소 관리는 사용자가 자주 사용하는 주소(집, 회사 등)를 저장하고 관리하는 기능입니다. 주요 기능은:

1. **주소 CRUD**: 등록, 조회, 수정, 삭제
2. **기본 주소 설정**: 한 개의 기본 주소 지정 (지도 초기 위치로 사용)
3. **거리 계산**: 사용자 현재 위치로부터 각 주소까지의 거리 자동 계산
4. **주소 검색**: 주소 검색 및 과거 예약 이력에서 추가
5. **다중 입력 방식**: 다음API, 지도, 검색 결과 3가지 방법 지원

**파일 경로:**
- `src/pages/user/owner/MyPage/Address/AddressManagePage.jsx` - 메인 페이지
- `src/pages/user/owner/MyPage/Address/components/AddressFormModal.jsx` - 등록/수정 폼
- `src/pages/user/owner/MyPage/Address/components/MapModal.jsx` - 지도 선택
- `src/services/addressService.js` - API 호출

**꼬리질문 4-1-1:** 기본 주소는 어떻게 활용되나요?
> **답변:** 기본 주소는 3가지 용도로 사용됩니다:
> 1. 지도 페이지의 초기 중심 위치
> 2. 예약 시 기본 선택 주소
> 3. 배송지 등 서비스 제공 주소
>
> localStorage에 동기화하여 다른 탭에서도 동일한 기본 주소를 사용합니다.

**꼬리질문 4-1-2:** 거리 계산은 어떻게 하나요?
> **답변:** 서버에서 **Haversine 공식**을 사용해 두 좌표 간의 직선 거리를 계산합니다. 클라이언트는 현재 위치(GPS)를 API 파라미터로 전달하고, 응답에 `distanceKm` 필드를 받아 표시합니다.
> ```javascript
> const addresses = await getAddresses({
>   userLat: currentLocation.latitude,
>   userLng: currentLocation.longitude
> });
> ```

---

### Q4-2: 주소 입력 방식을 3가지나 지원한 이유는?

**답변:**
각 방식마다 장단점이 있어 사용자 편의를 위해 모두 지원했습니다:

1. **다음 우편번호 API**
   - 장점: 표준 주소 형식, 정확한 우편번호
   - 단점: 신축 건물이나 특수 주소는 누락 가능

2. **지도에서 선택 (MapModal)**
   - 장점: 정확한 좌표, 시각적으로 위치 확인
   - 단점: 주소 입력이 번거로울 수 있음

3. **검색 결과 선택**
   - 장점: 빠른 재사용 (과거 예약 이력)
   - 단점: 새 주소는 입력 불가

특히 **MapModal**은 다음API로 찾기 어려운 주소(산속, 공원 등)를 정확한 좌표로 저장할 수 있어 유용합니다.

**꼬리질문 4-2-1:** 우선순위는 어떻게 정했나요?
> **답변:** 좌표 정확도를 우선시했습니다:
> 1. 지도 선택 (사용자가 직접 확인)
> 2. 다음 API (표준 주소)
> 3. 검색 결과 (기존 데이터 재사용)
> 4. 기존 좌표 (수정 모드)

**꼬리질문 4-2-2:** 3가지 방식을 통합한 컴포넌트 구조는?
> **답변:**
> - **AddressFormModal**: 3가지 입력 방식을 통합하는 메인 모달
> - **MapModal**: 지도 선택 모달 (서브 모달)
> - **다음API**: 직접 호출 (팝업)
>
> 각 방식으로 입력된 데이터를 `AddressFormModal`의 state로 병합하여 최종 저장합니다.

---

### Q4-3: MapModal에서 주소와 좌표를 어떻게 동기화하나요?

**답변:**
MapModal은 **역지오코딩**(좌표 → 주소)을 사용해 동기화합니다:

1. 사용자가 마커를 드래그하거나 지도를 클릭
2. 새 위치의 좌표를 얻음
3. 카카오맵 Geocoder의 `coord2Address()` 호출
4. 결과에서 도로명 주소 또는 지번 주소 추출
5. 주소와 우편번호를 state에 업데이트

```javascript
window.kakao.maps.event.addListener(marker, 'dragend', function() {
  const position = marker.getPosition();
  const geocoder = new window.kakao.maps.services.Geocoder();

  geocoder.coord2Address(position.getLng(), position.getLat(), (result, status) => {
    if (status === window.kakao.maps.services.Status.OK) {
      const roadAddr = result[0].road_address?.address_name || result[0].address.address_name;
      const postcode = result[0].road_address?.zone_no || '';

      setSelectedLocation({
        address: roadAddr,
        postcode: postcode,
        latitude: position.getLat(),
        longitude: position.getLng()
      });
    }
  });
});
```

**파일:** `src/pages/user/owner/MyPage/Address/components/MapModal.jsx`

**꼬리질문 4-3-1:** 역지오코딩이 실패하면 어떻게 하나요?
> **답변:** 지번 주소를 대체로 사용하고, 그것도 없으면 "주소 확인 불가"로 표시합니다. 좌표는 유지되므로 거리 계산 등의 기능은 정상 동작합니다.

**꼬리질문 4-3-2:** 도로명 주소와 지번 주소 중 어떤 것을 우선하나요?
> **답변:** 도로명 주소를 우선합니다. 2014년부터 도로명 주소가 공식 주소이고, 우편번호도 도로명 기준이기 때문입니다. 다만 도로명 주소가 없는 경우(산간 지역 등)에는 지번 주소를 사용합니다.

---

### Q4-4: 기본 주소 변경 시 다른 페이지에는 어떻게 반영되나요?

**답변:**
기본 주소 변경 시 **localStorage**와 **storage 이벤트**를 사용해 동기화합니다:

```javascript
// 기본 주소 설정
const handleSetDefaultAddress = async (addressId) => {
  await setDefaultAddress(addressId);

  // localStorage 업데이트
  const updatedAddress = addresses.find(addr => addr.id === addressId);
  localStorage.setItem('defaultAddress', JSON.stringify(updatedAddress));

  // storage 이벤트 발생 (다른 탭 동기화)
  window.dispatchEvent(new Event('storage'));

  loadAddresses(); // 현재 페이지 새로고침
};

// 다른 페이지에서 감지
useEffect(() => {
  const handleStorageChange = () => {
    const defaultAddress = JSON.parse(localStorage.getItem('defaultAddress'));
    if (defaultAddress) {
      setUserLocation({
        latitude: defaultAddress.latitude,
        longitude: defaultAddress.longitude
      });
    }
  };

  window.addEventListener('storage', handleStorageChange);
  return () => window.removeEventListener('storage', handleStorageChange);
}, []);
```

**꼬리질문 4-4-1:** localStorage 대신 Context API를 사용할 수도 있지 않나요?
> **답변:** Context API는 같은 React 트리 내에서만 동작합니다. 하지만 사용자가 여러 탭을 열 수 있고, 새로고침 후에도 기본 주소를 유지해야 하므로 localStorage가 더 적합합니다. 추가로 Context와 localStorage를 함께 사용하면 더 좋습니다.

**꼬리질문 4-4-2:** Redux를 사용하지 않은 이유는?
> **답변:** 이 프로젝트는 전역 상태가 많지 않고 (사용자 정보, 기본 주소 정도), 컴포넌트 간 데이터 공유가 복잡하지 않아 Redux를 도입하지 않았습니다. localStorage와 Context API만으로 충분했고, 번들 크기도 줄일 수 있었습니다.

---

## 5. 트러블 슈팅

### Q5-1: 마커 중복 문제는 어떻게 해결했나요?

**문제 상황:**
서비스 필터를 변경할 때마다 새 마커가 추가되지만 기존 마커는 제거되지 않아 화면에 마커가 계속 쌓이는 문제가 발생했습니다.

**원인 분석:**
마커를 지도에 추가할 때 `marker.setMap(map)`을 호출하지만, 필터 변경 시 기존 마커를 제거하지 않았습니다. 카카오맵 API는 자동으로 마커를 제거하지 않으므로 명시적으로 `setMap(null)`을 호출해야 합니다.

**해결 방법:**
1. **마커 배열을 useRef로 관리**: 현재 표시된 마커들을 추적
2. **필터 변경 시 기존 마커 제거**: `setMap(null)` 호출
3. **새 마커 렌더링**: 필터링된 업체 목록으로 새 마커 생성

```javascript
// 기존 마커 제거 함수
const clearMarkers = useCallback(() => {
  markersRef.current.forEach(marker => marker.setMap(null));
  labelsRef.current.forEach(label => label.setMap(null));
  markersRef.current = [];
  labelsRef.current = [];
}, []);

// 필터 변경 시
useEffect(() => {
  clearMarkers(); // 기존 마커 제거
  renderMarkers(filteredCompanies); // 새 마커 렌더링
}, [filteredCompanies]);
```

**파일:** `src/pages/common/Map/components/MapContainer.jsx`

**결과:**
필터 변경 시 기존 마커가 깔끔하게 제거되고 새 마커만 표시되어 UI가 정상 동작합니다.

**꼬리질문 5-1-1:** useRef 대신 useState를 사용하면 어떻게 되나요?
> **답변:** useState를 사용하면 마커 배열이 변경될 때마다 컴포넌트가 리렌더링됩니다. 마커는 카카오맵 API의 네이티브 객체이므로 React의 렌더링 사이클과 무관하게 관리되어야 하고, useRef를 사용하면 불필요한 리렌더링을 방지할 수 있습니다.

**꼬리질문 5-1-2:** 메모리 누수는 없나요?
> **답변:** 컴포넌트 언마운트 시 `useEffect` cleanup 함수에서 `clearMarkers()`를 호출하여 모든 마커를 제거합니다. 또한 마커 배열을 비워 참조를 해제하므로 메모리 누수가 없습니다.
> ```javascript
> useEffect(() => {
>   return () => {
>     clearMarkers();
>     if (userMarkerRef.current) {
>       userMarkerRef.current.setMap(null);
>     }
>   };
> }, []);
> ```

---

### Q5-2: 비동기 초기 로딩 문제는 어떻게 해결했나요?

**문제 상황:**
사용자의 기본 주소를 조회하기 전에 지도가 렌더링되어, 지도 중심이 기본값(서울시청)으로 설정되었다가 주소 조회 후 다시 이동하는 현상이 발생했습니다. 사용자 경험이 좋지 않았습니다.

**원인 분석:**
`useEffect`가 두 번 실행되는 문제:
1. 첫 번째: `isKakaoLoaded`가 true가 되면 지도 초기화 (userLocation은 아직 null)
2. 두 번째: `userLocation`이 설정되면 지도 중심 이동

**해결 방법:**
1. **의존성 배열 조정**: `useEffect([isKakaoLoaded, userLocation])`로 두 조건 모두 만족할 때만 실행
2. **로딩 상태 관리**: 주소 조회 중에는 지도를 렌더링하지 않음
3. **초기화 플래그**: `isInitializedRef`로 중복 초기화 방지

```javascript
// 주소 조회
useEffect(() => {
  if (!isKakaoLoaded || initOnceRef.current) return;
  initOnceRef.current = true;

  const initializeLocation = async () => {
    if (user?.userId) {
      try {
        const defaultAddress = await getAddressesByDefault(user.userId);
        if (defaultAddress?.latitude && defaultAddress?.longitude) {
          setUserLocation({
            latitude: defaultAddress.latitude,
            longitude: defaultAddress.longitude
          });
          return; // 성공하면 GPS 시도하지 않음
        }
      } catch (e) {
        console.log("기본주소 로드 실패, GPS 시도");
      }
    }

    // GPS fallback
    if (navigator.geolocation) {
      navigator.geolocation.getCurrentPosition((pos) => {
        setUserLocation({
          latitude: pos.coords.latitude,
          longitude: pos.coords.longitude
        });
      });
    }
  };

  initializeLocation();
}, [isKakaoLoaded, user]);

// 지도 초기화 (userLocation이 설정된 후에만)
useEffect(() => {
  if (!isKakaoLoaded || !userLocation || isInitializedRef.current) return;

  const container = document.getElementById("map");
  const options = {
    center: new window.kakao.maps.LatLng(userLocation.latitude, userLocation.longitude),
    level: 3
  };
  const map = new window.kakao.maps.Map(container, options);
  mapRef.current = map;
  isInitializedRef.current = true;
}, [isKakaoLoaded, userLocation]);
```

**파일:** `src/pages/common/Map/MapPage.jsx`

**결과:**
주소 조회가 완료된 후 지도가 한 번에 정확한 위치에 렌더링되어 사용자 경험이 개선되었습니다.

**꼬리질문 5-2-1:** 주소 조회가 오래 걸리면 어떻게 되나요?
> **답변:** 현재는 로딩 인디케이터를 표시하지 않지만, 개선 방안으로 스피너를 추가할 수 있습니다:
> ```javascript
> if (!isKakaoLoaded || !userLocation) {
>   return <div className="loading-spinner">지도를 불러오는 중...</div>;
> }
> ```

**꼬리질문 5-2-2:** GPS 권한을 거부하면 어떻게 되나요?
> **답변:** GPS 실패 시 기본값(서울시청)으로 fallback합니다. 추가로 사용자에게 "위치 권한을 허용하면 더 정확한 서비스를 받을 수 있습니다"와 같은 안내를 표시할 수 있습니다.

---

### Q5-3: 필터 리렌더링 최적화는 어떻게 했나요?

**문제 상황:**
서비스 필터 버튼을 클릭할 때마다 전체 컴포넌트가 리렌더링되어 지도가 깜빡이고 성능이 저하되는 문제가 발생했습니다.

**원인 분석:**
`selectedService` state가 변경되면 `MapPage` 컴포넌트가 리렌더링되고, 자식 컴포넌트들(MapContainer, CompanyListSidebar 등)도 모두 리렌더링되었습니다. 특히 업체 필터링 로직이 매번 실행되어 비효율적이었습니다.

**해결 방법:**
1. **useMemo로 필터링 결과 메모이제이션**: 의존성이 변경될 때만 재계산
2. **useCallback으로 핸들러 메모이제이션**: 자식 컴포넌트의 불필요한 리렌더링 방지
3. **컴포넌트 분리**: 필터 UI와 지도 컴포넌트를 분리하여 영향 범위 최소화

```javascript
// 필터링 결과 메모이제이션
const displayCompanies = useMemo(() => {
  const baseCompanies = filteredCompanies.length > 0 ? filteredCompanies : companies;
  if (selectedService === null) {
    return baseCompanies;
  }
  return baseCompanies.filter(company => company.repService === selectedService);
}, [filteredCompanies, companies, selectedService]);

// 핸들러 메모이제이션
const handleServiceFilter = useCallback((serviceId) => {
  setSelectedService(serviceId);
  setCurrentPage(1);
  setSelectedCompany(null);
}, []);

const handleLocationChange = useCallback((lat, lng, zoomLevel) => {
  loadNearbyCompanies(lat, lng, zoomLevel);
}, [loadNearbyCompanies]);
```

**파일:** `src/pages/common/Map/MapPage.jsx`

**결과:**
필터 변경 시 필요한 부분만 업데이트되고, 지도는 리렌더링되지 않아 성능이 크게 개선되었습니다.

**꼬리질문 5-3-1:** useMemo의 비용이 더 클 수도 있지 않나요?
> **답변:** useMemo는 의존성 비교 비용이 있지만, 이 경우 필터링 로직(배열 순회)이 더 비용이 큽니다. 업체가 수십 개 이상일 때는 useMemo가 확실히 유리합니다. 다만 업체가 10개 미만이면 useMemo 없이도 충분합니다.

**꼬리질문 5-3-2:** React.memo는 사용하지 않았나요?
> **답변:** 일부 자식 컴포넌트(CompanyCard, MapContainer)에 React.memo를 적용했습니다. 특히 MapContainer는 지도 렌더링 비용이 크므로 props가 변경되지 않으면 리렌더링하지 않도록 최적화했습니다.
> ```javascript
> export default React.memo(MapContainer, (prevProps, nextProps) => {
>   return prevProps.companies === nextProps.companies &&
>          prevProps.selectedCompany === nextProps.selectedCompany;
> });
> ```

---

### Q5-4: 그 외 다른 문제는 없었나요?

**문제 1: API 호출 중복**
- **상황**: 지도 드래그 시 API가 여러 번 호출되는 문제
- **해결**: 디바운싱(500ms)으로 마지막 호출만 실행

**문제 2: 마커 클릭 이벤트 중복**
- **상황**: 마커 재생성 시 이벤트 리스너가 중복 등록됨
- **해결**: `removeListener()` 호출 후 재등록, 또는 마커 재사용

**문제 3: 좌표 정밀도 손실**
- **상황**: 좌표를 소수점 5자리로 저장하여 정확도가 낮음
- **해결**: 소수점 7자리(약 1m 정밀도)로 저장

**문제 4: 크로스 브라우징 이슈**
- **상황**: Safari에서 카카오맵 스크립트 로딩 실패
- **해결**: `async` 대신 `defer` 속성 사용, 또는 동적 로딩

**꼬리질문 5-4-1:** 성능 모니터링은 어떻게 했나요?
> **답변:** React DevTools Profiler로 컴포넌트 렌더링 시간을 측정하고, Chrome DevTools Performance 탭으로 전체 성능을 분석했습니다. 특히 지도 드래그 시 프레임 드롭을 확인하고 최적화했습니다.

**꼬리질문 5-4-2:** 에러 모니터링은 어떻게 했나요?
> **답변:** 현재는 `console.error`로 로깅만 하지만, 프로덕션 환경에서는 Sentry와 같은 에러 트래킹 서비스를 도입할 계획입니다. 특히 API 호출 실패나 카카오맵 로딩 실패를 모니터링하는 것이 중요합니다.

---

## 6. 기술적 의사결정

### Q6-1: 카카오맵 API를 선택한 이유는?

**답변:**
국내 지도 서비스 중 카카오맵을 선택한 이유는:

1. **한국 지역 데이터 정확도**: 네이버맵과 함께 가장 정확한 국내 지도 데이터 제공
2. **무료 사용량**: 일일 30만 건까지 무료 (초기 단계에 적합)
3. **풍부한 문서와 예제**: 공식 문서와 커뮤니티 자료가 많음
4. **추가 서비스 통합**: 주소 검색, 장소 검색 등 부가 기능 제공
5. **개발 편의성**: JavaScript SDK가 직관적이고 React와 호환성 좋음

**대안:**
- **네이버맵**: 국내 점유율 1위, 더 상세한 지도 데이터
- **구글맵**: 글로벌 서비스에 적합, 해외 확장 시 유리

**꼬리질문 6-1-1:** 네이버맵 대신 카카오맵을 선택한 결정적 이유는?
> **답변:** 가격 정책이 더 저렴하고(네이버맵은 월 10만 건), API 문서가 더 직관적이었습니다. 또한 다음 우편번호 API와 자연스럽게 통합할 수 있다는 점도 큰 장점이었습니다.

**꼬리질문 6-1-2:** 카카오맵의 제약사항은 없었나요?
> **답변:** 무료 사용량(일 30만 건)을 초과하면 과금이 발생합니다. 트래픽이 많아지면 비용이 부담될 수 있으므로, 향후 캐싱 전략이나 타일 서버 자체 호스팅을 고려할 수 있습니다.

---

### Q6-2: 상태 관리 라이브러리를 사용하지 않은 이유는?

**답변:**
이 프로젝트는 Redux나 Zustand 같은 전역 상태 관리 라이브러리를 사용하지 않고, React의 내장 기능(`useState`, `useContext`, `localStorage`)만으로 상태를 관리했습니다.

**이유:**
1. **전역 상태가 적음**: 사용자 정보, 기본 주소 정도만 전역으로 관리
2. **컴포넌트 깊이가 얕음**: props drilling이 심하지 않음
3. **학습 곡선 최소화**: 팀원들이 Redux를 잘 모르면 개발 속도 저하
4. **번들 크기 절약**: Redux는 약 10KB의 추가 용량

**트레이드오프:**
- 장점: 간단하고 빠른 개발, 가벼운 번들
- 단점: 전역 상태가 많아지면 관리가 어려워짐

**꼬리질문 6-2-1:** 프로젝트가 커지면 어떻게 대응할 건가요?
> **답변:** Context API를 먼저 도입하고, 성능 이슈가 발생하면 Zustand나 Jotai 같은 경량 라이브러리를 고려할 것입니다. Redux는 너무 무겁고 보일러플레이트가 많아 최후의 수단으로 고려합니다.

**꼬리질문 6-2-2:** Context API의 성능 문제는 없었나요?
> **답변:** Context는 value가 변경되면 모든 구독자가 리렌더링되는 문제가 있습니다. 이를 방지하기 위해 Context를 기능별로 분리하고(UserContext, LocationContext), 자식 컴포넌트에 React.memo를 적용했습니다.

---

### Q6-3: JWT 토큰 관리 전략은?

**답변:**
JWT 인증은 다음과 같이 구현했습니다:

1. **액세스 토큰**: localStorage에 저장 (유효기간 1시간)
2. **리프레시 토큰**: httpOnly 쿠키에 저장 (유효기간 7일)
3. **axios 인터셉터**: 모든 요청에 액세스 토큰 자동 추가
4. **토큰 갱신**: 401 에러 시 리프레시 토큰으로 재발급 후 재시도

```javascript
// axios 인터셉터
apiRequest.interceptors.response.use(
  response => response,
  async error => {
    if (error.response?.status === 401 && !error.config._retry) {
      error.config._retry = true;

      try {
        const { data } = await axios.post('/auth/refresh', {}, { withCredentials: true });
        localStorage.setItem('accessToken', data.accessToken);
        error.config.headers['Authorization'] = `Bearer ${data.accessToken}`;
        return apiRequest(error.config);
      } catch (refreshError) {
        localStorage.removeItem('accessToken');
        window.location.href = '/login';
      }
    }
    return Promise.reject(error);
  }
);
```

**파일:** `src/services/api.js`

**꼬리질문 6-3-1:** localStorage는 XSS 공격에 취약하지 않나요?
> **답변:** 맞습니다. 이상적으로는 액세스 토큰도 httpOnly 쿠키에 저장하는 것이 더 안전합니다. 하지만 현재 구조에서는:
> 1. React 앱의 모든 API 호출에 토큰을 추가해야 함
> 2. httpOnly 쿠키는 JavaScript에서 접근 불가
> 3. CORS 설정 복잡도 증가
>
> 따라서 빠른 개발을 위해 localStorage를 선택했고, XSS 방어는 CSP(Content Security Policy) 헤더와 입력 검증으로 대응했습니다.

**꼬리질문 6-3-2:** 리프레시 토큰도 만료되면 어떻게 되나요?
> **답변:** 리프레시 토큰이 만료되면 자동으로 로그인 페이지로 리다이렉트됩니다. 사용자는 다시 로그인해야 하며, 이는 보안을 위한 정상적인 흐름입니다.

---

### Q6-4: 이미지 저장 방식은?

**답변:**
업체 사진은 **서버의 로컬 파일 시스템**에 저장하고, DB에는 **파일 경로(URL)**만 저장했습니다.

**저장 구조:**
```
/uploads/
  /companies/
    /123/
      - image1.jpg
      - image2.jpg
```

**장점:**
- 간단한 구현
- 추가 비용 없음
- 빠른 조회 속도 (같은 서버)

**단점:**
- 서버 스토리지 용량 제한
- 백업 및 복구 어려움
- CDN 없이는 전송 속도 느림

**개선 방안:**
- **AWS S3**: 무제한 스토리지, CDN 통합 (CloudFront)
- **이미지 최적화**: WebP 변환, 리사이징

**꼬리질문 6-4-1:** S3 대신 로컬 저장을 선택한 이유는?
> **답변:** 프로토타입 단계에서는 개발 속도와 비용을 우선시했습니다. S3는 설정이 복잡하고 초기 비용이 발생하므로, MVP 완성 후 마이그레이션 계획입니다.

**꼬리질문 6-4-2:** 이미지 URL은 절대 경로인가요, 상대 경로인가요?
> **답변:** 상대 경로(`/uploads/companies/123/image1.jpg`)로 저장하고, 프론트엔드에서 `process.env.REACT_APP_API_URL + imagePath`로 조합합니다. 이렇게 하면 서버 도메인이 변경되어도 DB를 수정할 필요가 없습니다.

---

### Q6-5: 개선하고 싶은 부분은?

**답변:**

1. **성능 최적화**
   - 이미지 lazy loading (Intersection Observer)
   - 마커 클러스터링 (많은 업체 표시 시)
   - 가상 스크롤 (업체 목록이 길 때)

2. **사용자 경험 개선**
   - 오프라인 지도 캐싱 (PWA)
   - 스켈레톤 UI (로딩 시)
   - 애니메이션 추가 (마커 등장, 페이지 전환)

3. **테스트 코드 작성**
   - Jest + React Testing Library
   - E2E 테스트 (Cypress)

4. **접근성 개선**
   - 키보드 네비게이션
   - 스크린 리더 지원
   - ARIA 속성 추가

5. **TypeScript 마이그레이션**
   - 타입 안정성 향상
   - 개발 생산성 향상

**꼬리질문 6-5-1:** 가장 우선순위가 높은 개선은?
> **답변:** **TypeScript 마이그레이션**입니다. 현재 API 응답 타입이 명확하지 않아 런타임 에러가 발생할 가능성이 높습니다. TypeScript를 도입하면 개발 시 에러를 미리 발견하고, 자동완성으로 생산성도 높일 수 있습니다.

**꼬리질문 6-5-2:** 시간이 부족하면 어떤 기능을 포기할 건가요?
> **답변:** 애니메이션이나 오프라인 캐싱 같은 **부가 기능**을 먼저 포기합니다. 핵심 기능(지도 검색, 예약)이 안정적으로 동작하는 것이 최우선이며, UX 개선은 이후 단계에서 점진적으로 추가할 수 있습니다.

---

## 7. 종합 질문

### Q7-1: 이 프로젝트에서 가장 어려웠던 부분은?

**답변:**
**마커 중복 문제와 비동기 초기 로딩 문제**를 동시에 해결하는 것이 가장 어려웠습니다.

두 문제 모두 React의 생명주기와 외부 라이브러리(카카오맵 API)의 통합에서 발생했고, useEffect의 의존성 배열을 정확히 설정하고 ref를 적절히 활용하는 것이 핵심이었습니다.

특히 마커가 쌓이는 문제는 처음에는 React의 버그라고 생각했지만, 실제로는 카카오맵 API의 동작 방식을 이해하지 못한 것이었습니다. 공식 문서를 꼼꼼히 읽고, 콘솔 로그로 디버깅하면서 해결했습니다.

---

### Q7-2: 이 프로젝트를 통해 배운 점은?

**답변:**

1. **외부 라이브러리 통합**: React와 외부 API(카카오맵, 다음 주소)를 통합하는 방법
2. **성능 최적화**: useMemo, useCallback, React.memo를 적절히 활용하는 방법
3. **비동기 처리**: Promise, async/await, useEffect의 실행 순서 제어
4. **상태 관리 전략**: 언제 state를 사용하고, 언제 ref를 사용할지 판단
5. **트러블 슈팅 능력**: 문제를 분석하고 체계적으로 해결하는 과정

---

### Q7-3: 다시 프로젝트를 시작한다면 어떻게 할 건가요?

**답변:**

1. **TypeScript 사용**: 처음부터 타입 안정성 확보
2. **테스트 주도 개발**: 핵심 로직에 단위 테스트 작성
3. **컴포넌트 설계 개선**: Atomic Design 패턴 적용
4. **API 명세 우선**: Swagger 문서를 먼저 작성하고 개발
5. **성능 측정**: 초기부터 Lighthouse로 성능 모니터링

하지만 **빠른 프로토타입 개발**이라는 목표를 고려하면, 현재 방식도 나쁘지 않았다고 생각합니다. 완벽한 코드보다 동작하는 제품을 빨리 만드는 것이 더 중요할 때도 있습니다.

---

## 📚 참고 자료

- [카카오맵 API 문서](https://apis.map.kakao.com/web/)
- [다음 우편번호 서비스](https://postcode.map.daum.net/guide)
- [React 공식 문서 - useEffect](https://react.dev/reference/react/useEffect)
- [React 공식 문서 - useMemo](https://react.dev/reference/react/useMemo)

---

## 마무리 팁

면접에서는 **구체적인 코드**와 **문제 해결 과정**을 이야기하는 것이 중요합니다. 단순히 "마커 중복 문제를 해결했습니다"보다는 "마커가 쌓이는 문제를 발견하고, 콘솔 로그로 마커 배열을 확인한 결과 clearMarkers()가 호출되지 않는 것을 발견해서 useEffect의 의존성 배열을 수정했습니다"처럼 구체적으로 설명하세요.

또한 **꼬리질문에 대비**하여 각 기술 선택의 트레이드오프, 대안, 개선 방안을 미리 생각해두는 것이 좋습니다.

**화이팅! 🚀**
