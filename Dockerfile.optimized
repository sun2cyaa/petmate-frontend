# 빌드된 파일만 사용하는 경량 버전
FROM nginx:alpine

# 로컬에서 미리 빌드된 파일 복사 (build 폴더)
COPY build /usr/share/nginx/html

# Nginx 설정 파일 복사
COPY nginx.conf /etc/nginx/conf.d/default.conf

# 포트 노출
EXPOSE 80

# Nginx 실행
CMD ["nginx", "-g", "daemon off;"]