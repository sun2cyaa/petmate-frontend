# 펫메이트 면접 핵심 답변 요약 (10~20초 버전)

## 🗺️ 지도 페이지 핵심 질문

### Q1: 지도 페이지 핵심 기능은?
**답변 (15초):**
사용자 위치 기반으로 근처 반려동물 업체를 검색하는 기능입니다. 카카오맵 API를 사용했고, 드래그 가능한 마커, 서비스별 필터, 줌 레벨에 따른 검색 반경 자동 조정을 구현했습니다.

---

### Q2: 카카오맵은 어떻게 초기화하나요?
**답변 (15초):**
동적으로 스크립트를 로드하고 `window.kakao.maps.load()`로 명시적 초기화했습니다. `isKakaoLoaded` 상태로 로딩 완료를 추적하고, 완료 후 지도를 렌더링합니다.

```javascript
script.src = `//dapi.kakao.com/v2/maps/sdk.js?appkey=${KEY}&autoload=false`;
script.onload = () => window.kakao.maps.load(() => setIsKakaoLoaded(true));
```

---

### Q3: 마커는 어떻게 관리하나요?
**답변 (20초):**
useRef로 마커 배열을 관리합니다. useState 대신 useRef를 쓴 이유는 마커가 카카오맵의 네이티브 객체라 리렌더링이 불필요하기 때문입니다. 필터 변경 시 `setMap(null)`로 기존 마커를 제거하고 새 마커를 렌더링합니다.

---

### Q4: 초기 위치는 어떻게 설정하나요?
**답변 (15초):**
우선순위는 1) 사용자 기본 주소(DB), 2) GPS 위치, 3) 서울시청입니다. async로 순차 시도하며 실패 시 다음 방법으로 fallback합니다.

---

### Q5: 필터링은 어떻게 구현했나요?
**답변 (15초):**
useMemo로 필터링 결과를 메모이제이션했습니다. companies 배열을 순회하는 비용이 크기 때문에 의존성이 변경될 때만 재계산하도록 최적화했습니다.

---

### Q6: 지도 드래그 시 업체는 어떻게 업데이트되나요?
**답변 (20초):**
dragend와 zoom_changed 이벤트 리스너를 등록하고, 디바운싱 500ms를 적용해 과도한 API 호출을 방지했습니다. 마지막 이벤트만 실행되도록 setTimeout과 clearTimeout을 사용했습니다.

---

### Q7: 줌 레벨에 따라 검색 반경이 어떻게 달라지나요?
**답변 (15초):**
줌 레벨 1-2는 1km, 3-4는 2km, 5-6은 3km, 7 이상은 4km로 설정했습니다. 확대할수록 가까운 업체만, 축소할수록 넓은 범위를 조회합니다.

---

### Q8: 마커 이미지는 어떻게 생성하나요?
**답변 (20초):**
Canvas API로 동적 생성합니다. 원형 배경을 그리고 그 위에 서비스별 아이콘을 얹은 후 `toDataURL()`로 변환해 마커 이미지로 사용합니다. 선택 상태에서는 물방울 모양으로 변경됩니다.

---

### Q9: useState 대신 useRef를 사용한 이유는?
**답변 (15초):**
마커는 카카오맵 API의 네이티브 객체라 React 렌더링 사이클과 무관합니다. useRef를 쓰면 참조만 유지하고 불필요한 리렌더링을 방지할 수 있습니다.

---

### Q10: 카카오맵을 선택한 이유는?
**답변 (20초):**
국내 지도 정확도가 높고, 일일 30만 건까지 무료이며, 문서가 풍부합니다. 네이버맵보다 가격 정책이 저렴하고, 다음 우편번호 API와 자연스럽게 통합할 수 있어 선택했습니다.

---

## 🔧 트러블 슈팅 핵심 질문

### Q11: 마커 중복 문제란?
**답변 (20초):**
필터 변경 시 기존 마커가 제거되지 않고 계속 쌓이는 문제였습니다. 카카오맵 API는 자동으로 마커를 제거하지 않아서 `setMap(null)`을 명시적으로 호출해야 했습니다. useRef로 마커 배열을 관리하고 clearMarkers 함수를 만들어 해결했습니다.

---

### Q12: 마커 중복 문제를 어떻게 해결했나요?
**답변 (20초):**
clearMarkers 함수를 만들어 기존 마커에 `setMap(null)`을 호출하고 배열을 초기화했습니다. 필터 변경 시 useEffect에서 clearMarkers를 먼저 실행한 후 새 마커를 렌더링하도록 수정했습니다.

```javascript
const clearMarkers = () => {
  markersRef.current.forEach(marker => marker.setMap(null));
  markersRef.current = [];
};
```

---

### Q13: 비동기 초기 로딩 문제란?
**답변 (20초):**
사용자 주소 조회 전에 지도가 렌더링되어 서울시청으로 설정됐다가 주소 조회 후 다시 이동하는 문제였습니다. useEffect 의존성에 userLocation을 추가하고, 두 조건이 모두 만족될 때만 지도를 초기화하도록 수정했습니다.

---

### Q14: 비동기 초기 로딩 문제를 어떻게 해결했나요?
**답변 (20초):**
useEffect를 두 개로 분리했습니다. 첫 번째는 주소 조회만, 두 번째는 지도 초기화만 담당합니다. `isKakaoLoaded && userLocation` 조건으로 두 개가 모두 준비됐을 때만 지도를 렌더링하고, initOnceRef로 중복 초기화를 방지했습니다.

---

### Q15: 필터 리렌더링 최적화는?
**답변 (20초):**
필터 버튼 클릭 시 전체 컴포넌트가 리렌더링되는 문제였습니다. useMemo로 필터링 결과를 메모이제이션하고, useCallback으로 핸들러를 메모이제이션했습니다. React.memo로 MapContainer 컴포넌트도 최적화해 지도가 리렌더링되지 않도록 했습니다.

---

### Q16: useMemo를 사용한 이유는?
**답변 (15초):**
필터링 로직이 배열 전체를 순회하기 때문에 비용이 큽니다. useMemo를 쓰면 의존성 변경 시에만 재계산하므로 불필요한 연산을 방지할 수 있습니다.

---

### Q17: 디바운싱은 어떻게 구현했나요?
**답변 (20초):**
useRef로 타이머 ID를 저장하고, 이벤트 발생 시 clearTimeout으로 이전 타이머를 취소한 후 새 타이머를 등록했습니다. 500ms 내에 이벤트가 반복되면 마지막 것만 실행됩니다.

```javascript
debounceTimerRef.current = setTimeout(() => {
  onLocationChange(lat, lng, zoom);
}, 500);
```

---

### Q18: API 호출 중복 문제는?
**답변 (15초):**
지도 드래그 시 API가 여러 번 호출되는 문제를 디바운싱 500ms로 해결했습니다. 마지막 호출만 실행되도록 setTimeout과 clearTimeout을 조합했습니다.

---

### Q19: 메모리 누수는 없나요?
**답변 (15초):**
useEffect cleanup 함수에서 clearMarkers()를 호출해 모든 마커를 제거하고 배열을 비웁니다. 디바운스 타이머도 clearTimeout으로 정리하므로 메모리 누수가 없습니다.

---

### Q20: 성능 측정은 어떻게 했나요?
**답변 (15초):**
React DevTools Profiler로 컴포넌트 렌더링 시간을 측정하고, Chrome DevTools Performance 탭으로 전체 성능을 분석했습니다. 지도 드래그 시 프레임 드롭을 확인하고 최적화했습니다.

---

## 🎯 추가 핵심 질문

### Q21: 가장 어려웠던 부분은?
**답변 (20초):**
마커 중복 문제와 비동기 로딩 문제를 동시에 해결하는 것이었습니다. React 생명주기와 카카오맵 API의 통합이 어려웠고, useEffect 의존성 배열과 ref를 정확히 이해하는 것이 핵심이었습니다.

---

### Q22: 프로젝트를 통해 배운 점은?
**답변 (20초):**
외부 라이브러리와 React 통합 방법, useMemo와 useCallback을 활용한 성능 최적화, 비동기 처리와 useEffect 실행 순서 제어를 배웠습니다. 특히 state와 ref의 사용 시기를 판단하는 능력이 향상됐습니다.

---

### Q23: 개선하고 싶은 부분은?
**답변 (20초):**
TypeScript 마이그레이션이 최우선입니다. API 응답 타입이 명확하지 않아 런타임 에러 가능성이 있습니다. 추가로 마커 클러스터링, 이미지 lazy loading, 테스트 코드 작성을 개선하고 싶습니다.

---

### Q24: 다시 한다면 어떻게 할 건가요?
**답변 (15초):**
처음부터 TypeScript를 사용하고, 컴포넌트 설계 시 Atomic Design 패턴을 적용하겠습니다. API 명세를 Swagger로 먼저 정의하고, 핵심 로직에는 단위 테스트를 작성하겠습니다.

---

### Q25: React.memo는 어디에 적용했나요?
**답변 (15초):**
MapContainer와 CompanyCard에 적용했습니다. 특히 MapContainer는 지도 렌더링 비용이 크므로 props 변경 시에만 리렌더링되도록 최적화했습니다.

---

## 💡 면접 TIP

### 답변 구조 (STAR 기법):
1. **Situation**: "필터 변경 시 마커가 쌓이는 문제가 있었습니다"
2. **Task**: "기존 마커를 제거해야 했습니다"
3. **Action**: "useRef로 마커 배열을 관리하고 clearMarkers 함수를 만들었습니다"
4. **Result**: "필터 변경 시 깔끔하게 마커가 업데이트됩니다"

### 자신있게 말하기:
- "구현했습니다" ✅ (단정적)
- "구현한 것 같습니다" ❌ (불확실)
- "~를 사용해서 ~를 해결했습니다" ✅ (구체적)
- "~로 했던 것 같아요" ❌ (모호)

### 코드 언급 시:
- 파일 경로 정확히: "MapContainer.jsx 파일의 566번째 줄에서..."
- 함수명 정확히: "clearMarkers 함수를 만들어..."
- 키워드 정확히: "useRef", "setMap(null)", "디바운싱"

### 꼬리질문 대비:
- 각 기술의 장단점 준비
- 대안 기술 준비 (useState vs useRef)
- 개선 방안 준비 (TypeScript, 테스트)

---

## 📝 암기 키워드

### 지도 페이지:
- **초기화**: 동적 스크립트 로드, autoload=false, 명시적 초기화
- **마커 관리**: useRef, setMap(null), clearMarkers
- **필터링**: useMemo, 메모이제이션, 불필요한 연산 방지
- **이벤트**: dragend, zoom_changed, 디바운싱 500ms
- **우선순위**: 기본주소 → GPS → 서울시청

### 트러블 슈팅:
- **마커 중복**: setMap(null) 명시적 호출, useRef 배열 관리
- **비동기 로딩**: useEffect 분리, 의존성 배열 조정, initOnceRef
- **성능 최적화**: useMemo, useCallback, React.memo
- **디바운싱**: setTimeout, clearTimeout, useRef 타이머 ID
- **메모리 관리**: cleanup 함수, 마커 제거, 배열 초기화

---

**면접 화이팅! 🚀**
